# React Bits + generate-to-fit assets — 2026-09-12

## Decision

| Choice | Locked to | Why |
|---|---|---|
| Structure/copy | Unchanged five-room IA and `landingCopy.ts` | Operator asked for same structure and copy. |
| React Bits | Starter CSS: `staggered-text`, `blur-highlight`, `depth-card` (Pro key `RBPP-`) | Leaf components, not marketing blocks (blocks would replace Motionskin sectioning). |
| Rejected RB | `chroma-card`, `rising-particles`, `pixel-reveal`, `glass-tiles` | Require Three.js; would re-inflate the landing budget we just left. |
| Assets | Generate-to-fit stills via Imagine + Higgsfield GPT Image 2 | Night for Night + depth of field (GrokFilm). Void/gold/parchment. No people/text. Higgsfield 2K hero (`2688×1520`, job `2989c37b`) bound as `/media/urania/hero-2k.png`. |
| Slots | `hero`, `enter`, `instrument-{threshold,selemene,folio}`, seven `lens-*` | One still per Motionskin image slot. Video motion remains Golden Portal. |

## Harmonization

Host wins: Cinzel/Satoshi, gold spotlight `rgba(230,184,77)`, 18px radius, reduced-motion respected on every RB component.

## Gate

No Inter, no generic SaaS gradient, no wellness vocabulary, Access OTP still the only app exit.

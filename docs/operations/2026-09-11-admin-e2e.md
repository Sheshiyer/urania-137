# Admin E2E receipt — 2026-09-11 (v0.6.3)

Live production proof for the admin slice after release `v0.6.3` / SHA `93f69e975eb12d5f941af4cfc57ae96b45298237`.

Evidence directory: `docs/operations/evidence/2026-09-11-admin-e2e/`.

## Release already shipped (not the remaining blocker)

| Check | Result |
|---|---|
| Tag / SHA | `v0.6.3` @ `93f69e9…` |
| Release run | `34581173011` success |
| Attestation | `urania.production-attestation.v1` verify → `ok: true`, `issues: []` |
| App deploy | `93caeef7-2566-49c7-94f8-e328c0be266b` |
| Landing deploy | `4850894c-16c7-46bd-9c85-9f800a0bd890` |
| Split-host smoke | landing `200`, app/Access `302` to `red-queen-4dfa` |

## R-8 / ISC-188 — platform-admin session

Fresh Access OTP as `sheshnarayan.iyer@gmail.com` on `app.urania.tryambakam.space`.

| Probe | Result |
|---|---|
| `GET /api/me` | `200` `{id: c04424f4-…, email: sheshnarayan.iyer@gmail.com}` |
| `GET /api/admin/session` | `200` `principal: platform-admin`, permissions include `admin:analytics`, `hasAdminAnalyticsRead: true` |
| Pages secret names | includes `CF_PLATFORM_ADMIN_EMAILS`, `SELEMENE_API_KEY`, `CHAT_PROXY_TOKEN`, alert secrets |

Artifact: `evidence/2026-09-11-admin-e2e/r8-session.json`.

## R-14 — corpus / D1 / R2 / Vectorize in browser

| Probe | Result |
|---|---|
| `#/admin-data` | renders owner-scoped Corpus Browser |
| `GET /api/corpus` | `200`, **53/53** |
| `GET /api/corpus/:sha` | `200`, R2 HTML body length **156849** (`Abdulmuiz`) |
| `GET /api/patterns/search?q=witness` | `200`, **10** Vectorize hits, `query_embedding: true` |
| `GET /api/folio` | `200`, **25** Folio readings |
| Remote D1 migrations | `0001`–`0011` applied (`No migrations to apply`) |
| D1 counts | users 1 · subjects 1 · catalogue_readings 53 · readings 25 · chat_sessions 18 |
| R2 bucket | `urania-137-corpus` object_count **53** |
| Vectorize | `urania-137-corpus-index` 384-dim cosine (last mutated 2026-08-12 ingest) |

Artifacts: `r14-admin-data.json`, `admin-data.png`.

## WitnessRun → Folio / narrator / engine

| Probe | Result |
|---|---|
| Subject | `Shesh` / `1991-08-13` present (Threshold not blocking) |
| `/api/selemene/health` via Pages | `200` engine `3.3.1`, 19 engines, 6 workflows |
| `/api/selemene/api/v1/engines` | `200` (production `SELEMENE_API_KEY` valid) |
| `POST /api/chat/interpret` L1 pattern | `200`, `degraded: false`, agent `aletheios`, model `selemene-llm-proxy` |
| Browser WitnessRun Ask | answered turn with source-grounded claims + interpretive synthesis labeled |
| `reading_interpretations` | **0 → 2** |
| Folio reopen | `#/readings` shows **25** canonical records; selected integrated-reading route resolves |

Artifacts: `selemene-proxy.json`, `witness-interpret.json`, `witness-ui2.json`, `witness-ui2.png`, `folio-source.json`.

## Still open (honest)

- ISC-143 archive deletion audit surface
- ISC-145/146 Access-group/`selemene-admin` durability (runtime elevation is `CF_PLATFORM_ADMIN_EMAILS`, proven; group-write credential still unused)
- Engine REQ-1/REQ-3 `daily-panchanga` mode (Selemene repo)
- M2 landing product polish, M3 relationship UI caller, M4 canonical 723 import, M5 continuous Vectorize loop, M6 AgentScope, M7 fast-follows
- Branch protection on `main` still none; production Environment required reviewers still 0 (governance snapshot = unprotected)

## Decision

Wave 2 was not the release blocker. Release already shipped. Admin can see and run the live stack on v0.6.3. Planning spine should stop saying “wave 2 blocked / release not dispatchable.”

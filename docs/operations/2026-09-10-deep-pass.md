# Deep pass — 2026-09-10

Live SHA `b47bfad` (then this receipt). Goal/vision vs GitHub Liber vs local `.planning/` vs the upgraded Selemene engine. Every claim below was probed this session.

## 1. Liber vs local

| Surface | State |
|---|---|
| `origin/main` | **in sync** with local `main` after this receipt's commits |
| Open GitHub issues / PRs | **0 / 0** — current work is the GSD spine, not Liber cards |
| GitHub About | homepage `https://urania.tryambakam.space`; description is graph-first (Vercel wording gone) |
| GitHub Actions | `ci.yml` red on post-Aug-15 commits until the typecheck/audit fix in this pass; **zero** `readiness.yml` or `release.yml` runs ever |
| Preview env secrets | `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN` only. **Missing** `PRODUCTION_TARGETS_JSON` and `LOCATION_REMEDIATION_MANIFEST_JSON` that `readiness.yml` requires |
| Production env secrets | CF tokens + Access smoke + `ALERT_PROBE_TOKEN`. Environment protection rules: **0** |
| Branch protection on `main` | **none** (`readiness.yml` still snapshots this and will record the gap) |

`.planning/` on GitHub now holds STATE / PROJECT / ROADMAP / REQUIREMENTS / phases / NEXT-WAVE. Historical `plan.md`, chat/threshold onboarding, and the graph-first plan are **SHIPPED provenance**.

## 2. Vision vs what is actually wired

North star (`goal.md`): a person enters through conversation, gets a reading without losing the thread, reopens it as a source-honest Folio, and can tell computation from witness.

| Capability | Wired? | Evidence this pass |
|---|---|---|
| Graph + chat threshold + Folio | **yes** (local + code on `main`) | SPA, ChatSheet, `/api/folio`, `/api/chat/*` |
| Threshold first-run | **yes** | `#/threshold` intercepts empty `GET /api/subjects` |
| WitnessRun activity cards (Pattern / Embodied / Synthesis / Navigate + custom text) | **yes locally** | Playwright L1 + browser Ask, non-degraded (~31–36s) |
| Narrator LLM proxy | **yes locally** | `NARRATOR_LLM_URL` + UA + Nebius/NVIDIA fallback; keys in `.dev.vars` (gitignored) and llm-proxy KV |
| Same inference on **production Pages** | **no** | `release.yml` never ran; prod still the Aug-15 Functions |
| CF Access on protected app | **yes** | `GET https://urania-137.pages.dev/api/me` → **302** Access login (`kid=df8a00b1…`) |
| Custom domain `/api/*` | **not the app** | `https://urania.tryambakam.space/api/me` → **404** (public landing host, split-host) |
| Corpus admin browser (53 readings) | **implemented, not production-deployed** | T-079 on `main`; live Pages SHA is older |
| Daily Sky Weather | **in-app interpreter ①** | Engine `daily-panchanga` still not a served mode (REQ-1). Could not re-verify generate: API key in local `.dev.vars` is **401 Invalid or expired** against live engine |
| 18-engine atlas / graph children | **stale vs engine** | Live `/health`: **19 engines**, 6 workflows, version **3.3.1**. Atlas `EXPECTED_ENGINE_IDS` is still 18 (no `financial-biosensor`). Capture engines remain named, not faked, on urania |
| Bridge engines (TS) | **6 live** | `/health/ready` `bridge_engines`: tarot, i-ching, enneagram, sacred-geometry, sigil-forge, raaga. `bridge_failed_engines` not enumerated here |
| Relationship product UI | **API yes, production UI caller no** | M3; ISC-189 deferred |
| Vectorize continuous-learning write loop | **no** | M5 unevidenced |
| AgentScope Dyad | **no** | M6 decided, unimplemented |
| `birth_profiles` | **no** | ISC-31 tracked fast-follow |
| Calculation `(0,0,Asia/Kolkata)` approximation | **still present** | M7 |

## 3. Endpoint pass (one query each, 2026-09-10)

Direct engine `https://selemene.tryambakam.space` (no Access):

| Endpoint | HTTP | Note |
|---|---|---|
| `GET /health` | 200 | `status=ok version=3.3.1 engines_loaded=19 workflows_loaded=6` |
| `GET /health/ready` | 200 | postgres/redis/orchestrator present; 6 TS bridge engines listed |
| `GET /api/v1/health` | 404 | not a route |
| `GET /engines/capabilities` | 404 | capability-parity branch is **not** on this live tag |
| `GET /api/v1/engines` | 401 | local `SELEMENE_API_KEY` rejected (`UNAUTHORIZED`) |
| `GET /api/v1/workflows` | 401 | same |
| `POST /api/v1/engines/panchanga/calculate` | 401 | same — **cannot** refresh SCHEMA-1 / REQ-1 live this pass |
| `POST /api/v1/engines/financial-biosensor/calculate` | 401 | 19th engine exists in count only |
| `POST /api/v1/assets/generate` (kundali / daily-panchanga / bogus / integrated-reading) | 401 | REQ-1/2 not re-probed with a valid key |
| `POST /api/v1/witness/interpret` | 401 | |

Urania:

| Endpoint | HTTP | Note |
|---|---|---|
| `GET https://urania-137.pages.dev/` | 200 | HTML app shell |
| `GET https://urania-137.pages.dev/api/me` | **302** | CF Access challenge (correct) |
| `GET https://urania.tryambakam.space/` | 200 | public landing |
| `GET https://urania.tryambakam.space/api/me` | 404 | landing has no Functions API |
| llm-proxy `GET /v1/models` | 405 | worker alive; chat completions is POST-only |

Local (this session, earlier): `/api/me` 200 `dev@urania.local`; `/api/admin/session` platform-admin; `/api/chat/interpret` L0 + L1 non-degraded.

## 4. Release path — why `release.yml` is not a button we can press yet

`release.yml` is fail-closed:

1. Exact 40-hex SHA + semver already in `package.json` (`0.6.2`)
2. `readiness_run_id` whose artifact `urania-readiness-$SHA` contains five files bound to **that** SHA
3. `npm run verify:ci` inside the job
4. Production Environment (currently **no** required reviewers)

Blockers found this pass:

1. **CI red** on HEAD before the typecheck (`LlmToolCall` unused) + `npm audit --audit-level=high` (vitest mocker + sharp/libheif via wrangler) fixes.
2. **No readiness run has ever succeeded** — GitHub shows zero `readiness.yml` / `release.yml` runs.
3. Preview environment is missing `PRODUCTION_TARGETS_JSON` and `LOCATION_REMEDIATION_MANIFEST_JSON`. `readiness.yml` exits 1 if either is unset. The location manifest must also have `subjectSha === RELEASE_SHA`.
4. Local engine API key is expired — production Pages may still have a working `SELEMENE_API_KEY` secret; this environment does not.
5. Wave 2 human gates (FV-188 live Access proof, R-9 admin query) remain open. They are **not** in `release.yml`'s machine gate, but they are in STATE.md as M1 blockers.

Dispatch policy for this pass: push the CI fix, then **dispatch `readiness.yml`** for the new SHA. Do **not** invent `release.yml` inputs. If readiness fails on missing secrets, that failure *is* the production-path evidence.

## 5. Honest next human actions (cannot be agent-only)

1. Put `PRODUCTION_TARGETS_JSON` and a HEAD-bound `LOCATION_REMEDIATION_MANIFEST_JSON` on the **preview** GitHub Environment (template: `ops/production-targets.example.json`).
2. Rotate `SELEMENE_API_KEY` in Pages production **and** local `.dev.vars` so engine probes work.
3. Fresh Access login for FV-188 if you still want that ISA gate closed.
4. After a green readiness artifact: `gh workflow run release.yml -f sha=<40hex> -f version=0.6.2 -f readiness_run_id=<id>`.

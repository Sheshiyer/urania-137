# 01 — Hygiene + release-path build (waves 0–1)

Phase: M1 — Production readiness → release
Status: Complete

History of shipped work (waves 0–1). All items `[x]`; no open work remains here.

- [x] H-1: Commit + push pending corpus work (`main == origin/main`; CI green for 4bad753, 2026-08-13).
- [x] H-2: Correct 723→53 doc truth + regen DEPENDENCY-GRAPH (53 readings: 51 Solo + 2 Synastry; cab60b1).
- [x] R-7: Bind Pages preview D1 `6b90d773…` isolated from prod `d57550ea…`; preview fail-closed on engine secrets.
- [x] R-1: `scripts/d1/backup.mjs` + restore-drill + isolation verifier (8 tests green; frozen `--production --targets --output`).
- [x] R-2: `scripts/ops/validate-targets.mjs` + `cutover.mjs` (11 tests green; `--confirm` required for prod mutation).
- [x] R-3: `scripts/verify/split-host-gates.mjs` + `scripts/ops/alert-probe.mjs` (6 tests green; `--deny-mutations` read-only).
- [x] R-4: `scripts/data/mark-subjects-for-review.mjs` + migrations 0010+0011 (5 tests green; chain 11/11).
- [x] R-5: `release-preflight.mjs` asserts ops scripts exist (21 vitest green; fails-closed on missing script).
- [x] R-6: `readiness.yml` + `scripts/verify/readiness.mjs` emitting the 5 artifacts `release.yml` downloads (6 tests green).

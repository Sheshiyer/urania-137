# 04 — Release (wave 4)

Phase: M1 — Production readiness → release
Status: Pending — production Environment approval required

Release dispatch is approval-gated (merge/deploy policy v1). No automatic execution.

- [ ] R-13: Dispatch `release.yml` with production Environment approval; attestation `subject SHA == deployed SHA`. *(approval-gated)*

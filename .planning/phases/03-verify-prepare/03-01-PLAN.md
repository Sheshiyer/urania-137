# 03 — Verify + prepare (wave 3)

Phase: M1 — Production readiness → release
Status: Pending — blocked on wave 2

Release-candidate verification and preparation. Sequential; each step gates the next.

- [ ] R-10: `npm run verify:ci` green on the merged release-candidate SHA (canonical CI from clean clone).
- [ ] R-11: `release.mjs --prepare minor --dry-run` → deterministic version bump, no tag/deploy.
- [ ] R-12: `release.mjs --dry-run` preflight passes all fail-closed guards.
